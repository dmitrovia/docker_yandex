COPY <файл/директория> <путь-внутри-контейнера>
# альтернативный вариант записи:
COPY ["<файл/директория>", "<путь-внутри-контейнера>"]
из локальной -> в контейнер

ENTRYPOINT command param1 param2
# или
ENTRYPOINT ["executable", "param1", "param2"]

Команда WORKDIR устанавливает рабочую директорию в образе

ENV - задаем переменные окружения (внутри ОС контейнера)

ARG JAR_FILE=target/*.jar - задаем значение аргумента
COPY ${JAR_FILE} app.jar